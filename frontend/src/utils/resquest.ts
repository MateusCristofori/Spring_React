export const BASE_URL =
  import.meta.env.VITE_BACKEND_URL ?? 'http://localhost:8080'
// nessa constante não estamos usando o endpoint das vendas. Vamos usar isso dentro do useEffect por meio do template string com crases (``).
